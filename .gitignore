# Local-only deliverables (regenerable / not for version control)
*.pdf
*.csv
*.xlsx
*.docx

# Pipeline scratch
.pipeline/

# Superpowers scratch (smoke scripts, brainstorm mockups, progress ledger)
.superpowers/

# Secrets — never commit
.env
.env.*
*.token
*.key
credentials*.json

# Local editor / agent settings
.claude/settings.local.json

# gstack operational tooling state (runtime scratch + local tokens — never commit)
.gstack/

# OS cruft
Thumbs.db
Desktop.ini
.DS_Store

# Node
node_modules/

# Eleventy build output
public/

# Local Netlify folder
.netlify
deno.lock
